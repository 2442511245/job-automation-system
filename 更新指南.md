# 🔄 版本更新管理指南

> 本文档教你如何管理产品版本、打包更新、通知买家。
> **此文件不要发给买家！**

---

## 一、版本号规范

使用 `主版本.次版本.修订版本` 格式（Semantic Versioning）：

| 变更类型 | 示例 | 什么时候 |
|----------|------|----------|
| 修订版本 +1 | 2.2.0 → 2.2.1 | Bug 修复、小优化 |
| 次版本 +1 | 2.2.0 → 2.3.0 | 新功能、功能增强 |
| 主版本 +1 | 2.2.0 → 3.0.0 | 重大架构变更、不兼容更新 |

当前版本见根目录 `VERSION` 文件。

---

## 二、日常开发的更新流程

### 每次修改代码后

```bash
# 1. 修改代码（app.py / config.py / tampermonkey 等）

# 2. 更新版本号
echo "2.3.0" > VERSION

# 3. 提交到 GitHub
git add -A
git commit -m "v2.3.0: 优化数据分析报表，新增XX图表"
git push origin main
```

### 更新了什么就写清楚

提交信息模板：
```
vX.Y.Z: 简短描述

- 新增：XXX 功能
- 修复：XXX 问题
- 优化：XXX 体验
```

---

## 三、打包更新包

### 3.1 全量包（给新买家）

直接把整个文件夹打成 ZIP（**删除这些文件再打**）：
- ❌ `营销文案.md`
- ❌ `发货说明.md`
- ❌ `更新指南.md`（本文件）
- ❌ `.git` 文件夹

### 3.2 增量更新包（给老买家）

只包含本次变更的文件，保持目录结构：

```
更新_v2.2.0_to_v2.3.0.zip
├── 如何更新.txt          ← 告诉买家怎么替换
├── VERSION               ← 新版本号
├── app.py                ← 如果改了
├── config.py             ← 如果改了
├── static/
│   └── dashboard.js      ← 如果改了
└── tampermonkey/
    └── boss-automation.user.js  ← 如果改了
```

### 如何更新.txt 模板：

```
📦 求职自动化系统 更新包
版本：v2.2.0 → v2.3.0

📋 本次更新内容：
- 优化了数据分析报表的展示效果
- 新增了XX统计图表
- 修复了XX问题

⚙️ 更新方法：
1. 关闭正在运行的 Flask 后端（关掉黑色命令行窗口）
2. 将本 ZIP 中的文件解压到你的安装目录，选择"覆盖"
3. 你的个人数据（settings.json、投递记录）不会受影响
4. 重新双击"一键启动.bat"即可

⚠️ 注意：
- 不要删除 ~/JobReports/ 目录（那是你的数据）
- 如果 Tampermonkey 脚本有更新，需要重新复制到浏览器扩展中
- 如有问题，联系客服获取帮助
```

---

## 四、通知买家

### 方式一：咸鱼群发（推荐）

咸鱼 APP → 消息 → 选择历史买家 → 发送：

```
🎉 求职自动化系统 v2.3.0 已发布！

更新内容：
✅ 优化数据分析报表
✅ 新增 XX 图表展示
✅ 修复了 XX 问题

📥 更新包下载：https://pan.baidu.com/s/xxxxx
🔑 提取码：xxxx

更新方法：解压覆盖原文件即可，个人数据不受影响。
```

### 方式二：建立买家群

推荐用微信群/QQ群，方便：
- 统一通知更新
- 买家之间交流使用心得
- 收集反馈和建议
- 提高复购和转介绍

### 方式三：仪表盘内提示

在 `config.py` 中有一个 `update_check_url` 设置项。你可以搭建一个简易的版本检查接口，买家打开仪表盘时自动提示有新版本。

最简实现（无需服务器）：
1. 在某个公开位置放一个 `latest_version.txt`（比如另一个 GitHub 仓库、或者 Gitee）
2. 文件内容就是最新版本号，如 `2.3.0`
3. 买家的 `update_check_url` 指向这个文件
4. 仪表盘自动对比本地版本和远程版本，不一致时提示更新

---

## 五、更新频率建议

| 阶段 | 频率 | 内容 |
|------|------|------|
| 初期（前 3 个月） | 每 1-2 周 | 快速修复 bug、优化体验 |
| 稳定期 | 每 1-2 月 | 新功能、功能增强 |
| 成熟期 | 每季度 | 重大版本更新 |

太频繁的更新会让买家觉得产品不稳定，太少则显得无人维护。

---

## 六、数据分析模块改进路线图（参考）

既然你觉得数据分析还不完善，这是建议的迭代方向：

### v2.3 - 数据增强
- [ ] 回复率按岗位类型分类统计
- [ ] 每日趋势折线图（发送量/已读率/回复率变化）
- [ ] 匹配度与回复率交叉分析
- [ ] 导出 Excel 功能

### v2.4 - 智能优化
- [ ] AI 自动分析低回复率原因
- [ ] 基于历史数据推荐最佳投递时段
- [ ] 招呼语 A/B 测试（对比不同模板效果）
- [ ] 岗位质量评分（公司活跃度、HR 响应速度）

### v3.0 - 多平台
- [ ] 支持猎聘、拉勾等更多招聘平台
- [ ] 跨平台数据汇总对比
- [ ] 统一的候选人画像管理
- [ ] 自定义投递策略（按行业/薪资/规模筛选）

---

## 七、文件变更影响范围参考

修改某个文件时，了解对买家的影响：

| 修改文件 | 买家需要做什么 | 影响个人数据？ |
|----------|---------------|---------------|
| `app.py` | 替换文件 + 重启 | ❌ 不影响 |
| `config.py` | 替换文件 + 重启 | ❌ 不影响（除非新增设置项） |
| `tampermonkey/*.js` | 替换文件 + 重装脚本 | ❌ 不影响 |
| `static/*` | 替换文件 | ❌ 不影响 |
| `templates/*` | 替换文件 | ❌ 不影响 |
| `requirements.txt` | 替换 + 运行 `pip install -r requirements.txt` | ❌ 不影响 |
| `~/JobReports/settings.json` | **绝不覆盖！** | ✅ 这是买家数据！ |

**核心原则：买家数据在 `~/JobReports/` 目录下，更新包永远不要包含这个目录。**
